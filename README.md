# CleanArchitectureExample
Tutorial to transform your "working" python code into scalable, testable code with separation of concerns using clean architecture.

pip3 install flask
pip3 install flask-restful
pip3 install flask-mysql
pip3 install python-dotenv
